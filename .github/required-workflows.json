{
  "schema_version": 2,
  "branches": {
    "dev": {
      "workflows": [
        {
          "name": "tests",
          "path": ".github/workflows/tests.yml",
          "classification": "required_push",
          "event": "push",
          "allowed_conclusions": ["success"],
          "required_jobs": []
        },
        {
          "name": "sql-integration",
          "path": ".github/workflows/sql-integration.yml",
          "classification": "required_push",
          "event": "push",
          "allowed_conclusions": ["success"],
          "required_jobs": [
        {
          "name": "core SQL integration (HTTP)",
          "allowed_conclusions": [
            "success"
          ]
        },
        {
          "name": "core SQL integration (native)",
              "allowed_conclusions": ["success"]
            },
            {
              "name": "authentication SQL integration (HTTP + native)",
              "allowed_conclusions": ["success"]
            }
          ],
          "conditional_jobs": [
            {
              "name": "destructive fault injection (HTTP + native)",
              "events": ["schedule", "workflow_dispatch"],
              "allowed_conclusions": ["success", "skipped"],
              "reason": "Destructive fault groups run nightly or by explicit manual dispatch, never on dev pushes."
            },
            {
              "name": "SQL integration stress (HTTP + native)",
              "events": ["schedule", "workflow_dispatch"],
              "allowed_conclusions": ["success", "skipped"],
              "reason": "Resource-pressure scenarios run nightly or by explicit manual dispatch, never on dev pushes."
            }
          ]
        }
      ],
      "conditional_checks": [
        {
          "name": "destructive fault injection (HTTP + native)",
          "kind": "job",
          "allowed_conclusions": ["neutral", "skipped"],
          "reason": "The fault job is intentionally conditional on scheduled and manual events."
        },
        {
          "name": "SQL integration stress (HTTP + native)",
          "kind": "job",
          "allowed_conclusions": ["neutral", "skipped"],
          "reason": "The stress job is intentionally conditional on scheduled and manual events."
        }
      ],
      "classified_non_push_workflows": [
        {
          "name": "publish",
          "path": ".github/workflows/publish.yml",
          "classification": "release_only",
          "events": ["workflow_dispatch", "release"],
          "reason": "Release publishing is intentionally branch, tag, and manual gated."
        }
      ]
    }
  }
}
